name: ci

permissions:
  contents: read

on:
  push:
    branches: [$default-branch]
  pull_request:

jobs:
  ci:
    uses: JakobMelchard/.github/.github/workflows/terraform.yml@main
    with:
      working-directory: .   # infra keeps its root under terraform/; most repos do not
